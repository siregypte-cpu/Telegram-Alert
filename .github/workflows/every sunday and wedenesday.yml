name: every sunday and wedenesday

on:
  schedule:
    # الأحد والأربعاء الساعة 3:00 عصراً بتوقيت مصر
    - cron: '0 13 * * 0,3'
  workflow_dispatch: # يتيح لك تجربة الإرسال الآن يدوياً

jobs:
  send_message:
    runs-on: ubuntu-latest
    steps:
      - name: Send Message
        run: |
          # ضبط توقيت مصر
          export TZ='Africa/Cairo'
          CUR_DATE=$(date +'%Y-%m-%d')
          CUR_TIME=$(date +'%H:%M:%S')
          
          # بيانات البوت والجروب الخاصة بك
          TOKEN="8305317876:AAHkPFr8idftz1Rgc6-tdCugaaZTrQsZnP4"
          CHAT_ID="-5216371452"
          
          # نص الرسالة المعدل بناءً على طلبك
          MESSAGE="🔔 *مواعيد الكورس*%0A%0Aولى امر الطالبة اميرة محمد السيد%0A*الموضوع:* تذكير بميعاد كورس الرياضيات يومى الاحد والاربعاء الساعه الثالثه عصرا%0A%0A🗓 *التاريخ:* $CUR_DATE%0A⏰ *الوقت:* $CUR_TIME"
          
          # تنفيذ عملية الإرسال
          curl -s -X POST "https://api.telegram.org/bot$TOKEN/sendMessage" \
          -d "chat_id=$CHAT_ID" \
          -d "text=$MESSAGE" \
          -d "parse_mode=Markdown"
